# Publication compliance eval suite

Tests journal-specific hard constraints such as font size, dimensions, vector text,
and editable exports.

## Tasks

- `tiny_font`: axis text below journal minimum.
- `missing_units`: physical quantities lack units.
- `non_vector_text`: text rasterized in vector export.

See `tasks.jsonl`.
