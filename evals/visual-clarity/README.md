# Visual clarity eval suite

Tests layout clearance, label collision, clipping, and reading order at final size.

## Tasks

- `legend_overlap`: legends must not overlap data or panels.
- `clipped_annotation`: marks and labels must not be clipped by panel edges.
- `tiny_font`: text must remain legible at final physical size.

See `tasks.jsonl`.
