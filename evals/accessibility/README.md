# Accessibility eval suite

Tests WCAG-aligned contrast, color redundancy, and colormap choices.

## Tasks

- `color_only_encoding`: critical distinctions must not rely on hue alone.
- `low_contrast`: text and graphical objects must meet contrast thresholds.
- `wrong_colormap`: rainbow/jet colormaps must not be used for quantitative data.

See `tasks.jsonl`.
